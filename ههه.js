document.addEventListener('DOMContentLoaded', function() {
    const azkarSabahBtn = document.getElementById('azkar-sabah-btn');
    const azkarSabahCount = document.getElementById('azkar-sabah-count');
    let count = 0;

    azkarSabahBtn.addEventListener('click', function() {
        count++;
        azkarSabahCount.textContent = count;
        setTimeout(function() {
            if (count > 0) {
                count--;
                azkarSabahCount.textContent = count;
            }
        }, 1000);
    });

    const azkarMasaaBtn = document.getElementById('azkar-masaa-btn');
    const azkarMasaaCount = document.getElementById('azkar-masaa-count');
    let masaaCount = 0;
    azkarMasaaBtn.addEventListener('click', function() {
        masaaCount++;
        azkarMasaaCount.textContent = masaaCount;
        setTimeout(function() {
            if (masaaCount > 0) {
                masaaCount--;
                azkarMasaaCount.textContent = masaaCount;
            }
        }, 1000);
    });

    const azkarNomBtn = document.getElementById('azkar-nom-btn');
    const azkarNomCount = document.getElementById('azkar-nom-count');
    let nomCount = 0;
    azkarNomBtn.addEventListener('click', function() {
        nomCount++;
        azkarNomCount.textContent = nomCount;
        setTimeout(function() {
            if (nomCount > 0) {
                nomCount--;
                azkarNomCount.textContent = nomCount;
            }
        }, 1000);
    });

    const azkarBaadaSalahBtn = document.getElementById('azkar-baada-salah-btn');
    const azkarBaadaSalahCount = document.getElementById('azkar-baada-salah-count');
    let baadaSalahCount = 0;
    azkarBaadaSalahBtn.addEventListener('click', function() {
        baadaSalahCount++;
        azkarBaadaSalahCount.textContent = baadaSalahCount;
        setTimeout(function() {
            if (baadaSalahCount > 0) {
                baadaSalahCount--;
                azkarBaadaSalahCount.textContent = baadaSalahCount;
            }
        }, 1000);
    });

    const azkarKhalaBtn = document.getElementById('azkar-khala-btn');
    const azkarKhalaCount = document.getElementById('azkar-khala-count');
    let khalaCount = 0;
    azkarKhalaBtn.addEventListener('click', function() {
        khalaCount++;
        azkarKhalaCount.textContent = khalaCount;
        setTimeout(function() {
            if (khalaCount > 0) {
                khalaCount--;
                azkarKhalaCount.textContent = khalaCount;
            }
        }, 1000);
    });

    const azkarAklBtn = document.getElementById('azkar-akl-btn');
    const azkarAklCount = document.getElementById('azkar-akl-count');
    let aklCount = 0;
    azkarAklBtn.addEventListener('click', function() {
        aklCount++;
        azkarAklCount.textContent = aklCount;
        setTimeout(function() {
            if (aklCount > 0) {
                aklCount--;
                azkarAklCount.textContent = aklCount;
            }
        }, 1000);
    });

    const azkarSafarBtn = document.getElementById('azkar-safar-btn');
    const azkarSafarCount = document.getElementById('azkar-safar-count');
    let safarCount = 0;
    azkarSafarBtn.addEventListener('click', function() {
        safarCount++;
        azkarSafarCount.textContent = safarCount;
        setTimeout(function() {
            if (safarCount > 0) {
                safarCount--;
                azkarSafarCount.textContent = safarCount;
            }
        }, 1000);
    });

    const azkarIstiqazBtn = document.getElementById('azkar-istiqaz-btn');
    const azkarIstiqazCount = document.getElementById('azkar-istiqaz-count');
    let istiqazCount = 0;
    azkarIstiqazBtn.addEventListener('click', function() {
        istiqazCount++;
        azkarIstiqazCount.textContent = istiqazCount;
        setTimeout(function() {
            if (istiqazCount > 0) {
                istiqazCount--;
                azkarIstiqazCount.textContent = istiqazCount;
            }
        }, 1000);
    });

    const azkarDakhelBtn = document.getElementById('azkar-dakhel-btn');
    const azkarDakhelCount = document.getElementById('azkar-dakhel-count');
    let dakhelCount = 0;
    azkarDakhelBtn.addEventListener('click', function() {
        dakhelCount++;
        azkarDakhelCount.textContent = dakhelCount;
        setTimeout(function() {
            if (dakhelCount > 0) {
                dakhelCount--;
                azkarDakhelCount.textContent = dakhelCount;
            }
        }, 1000);
    });

    const azkarMasjedBtn = document.getElementById('azkar-masjed-btn');
    const azkarMasjedCount = document.getElementById('azkar-masjed-count');
    let masjedCount = 0;
    azkarMasjedBtn.addEventListener('click', function() {
        masjedCount++;
        azkarMasjedCount.textContent = masjedCount;
        setTimeout(function() {
            if (masjedCount > 0) {
                masjedCount--;
                azkarMasjedCount.textContent = masjedCount;
            }
        }, 1000);
    });

    const azkarHajjBtn = document.getElementById('azkar-hajj-btn');
    const azkarHajjCount = document.getElementById('azkar-hajj-count');
    let hajjCount = 0;
    azkarHajjBtn.addEventListener('click', function() {
        hajjCount++;
        azkarHajjCount.textContent = hajjCount;
        setTimeout(function() {
            if (hajjCount > 0) {
                hajjCount--;
                azkarHajjCount.textContent = hajjCount;
            }
        }, 1000);
    });

    const azkarMardBtn = document.getElementById('azkar-mard-btn');
    const azkarMardCount = document.getElementById('azkar-mard-count');
    let mardCount = 0;
    azkarMardBtn.addEventListener('click', function() {
        mardCount++;
        azkarMardCount.textContent = mardCount;
        setTimeout(function() {
            if (mardCount > 0) {
                mardCount--;
                azkarMardCount.textContent = mardCount;
            }
        }, 1000);
    });

    const azkarIstikharaBtn = document.getElementById('azkar-istikhara-btn');
    const azkarIstikharaCount = document.getElementById('azkar-istikhara-count');
    let istikharaCount = 0;
    azkarIstikharaBtn.addEventListener('click', function() {
        istikharaCount++;
        azkarIstikharaCount.textContent = istikharaCount;
        setTimeout(function() {
            if (istikharaCount > 0) {
                istikharaCount--;
                azkarIstikharaCount.textContent = istikharaCount;
            }
        }, 1000);
    });

    const azkarKarbBtn = document.getElementById('azkar-karb-btn');
    const azkarKarbCount = document.getElementById('az


